<!--components/textList/cmp.wxml-->
<view class='container {{listType=="between" ? "between" : ""}}'>
  <view class='title {{titleColor}}'>{{listTitle}}：</view>
  <view class='content {{contentColor}}'>{{listContent}}</view>
</view>